import { GlassCard } from '../design-system/index.js';

// Placeholder — recebe o conteúdo real (dados de /api/overview) na Task 11.
export default function Overview() {
  return <GlassCard style={{ padding: 24 }}>Overview — em construção</GlassCard>;
}
